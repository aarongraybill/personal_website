# Project Instructions

## Deployment boundary

- Do not deploy or push changes to Firebase.
- For website changes, stop after local checks and a successful `quarto render`.
- Firebase emulators may be used for local validation, but production Firebase state must remain unchanged.
- Only deploy to Firebase if user explicitly overrides this instruction in a later request.
